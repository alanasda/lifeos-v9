/** UI-only shell module. */
'use strict';
function renderEnergiaMental(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Energia</h2></div>${_emptyState('Sem sessões','Timer e histórico estão desativados no shell visual.')}</section>`;}
