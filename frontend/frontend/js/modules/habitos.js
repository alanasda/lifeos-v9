/** UI-only shell module. */
'use strict';
function renderHabitos(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Hábitos</h2></div>${_emptyState('Nenhum hábito','Lista vazia até o backend ser conectado.')}</section>`;}
