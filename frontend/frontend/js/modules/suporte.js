/** UI-only shell module. */
'use strict';
function renderSuporte(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Suporte</h2></div>${_emptyState('Suporte visual','Canais serão conectados futuramente.')}</section>`;}
