/** UI-only shell module. */
'use strict';
function renderPerfil(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Perfil</h2></div>${_emptyState('Perfil sem dados','Nome, e-mail e profissão virão do backend.')}</section>`;}
