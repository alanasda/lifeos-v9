/** UI-only shell module. */
'use strict';
function renderNovaRotina(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Rotina</h2></div>${_emptyState('Rotina vazia','Nenhuma tarefa local será inventada.')}</section>`;}
