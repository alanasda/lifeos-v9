/** UI-only shell module. */
'use strict';
function renderFeedback(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Feedback</h2></div>${_emptyState('Feedback visual','Envio para API desativado neste shell.')}</section>`;}
