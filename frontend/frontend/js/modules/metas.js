/** UI-only shell module. */
'use strict';
function renderMetasInteligentes(stage){stage.innerHTML=`<section class="card"><div class="section-head"><h2>Metas</h2></div>${_emptyState('Nenhuma meta','Metas reais serão carregadas futuramente.')}</section>`;}
